# 하냥냥 Backend 작업 규칙

## 변경 승인

- 사용자가 코드 또는 설정 변경을 명시적으로 요청하기 전에는 프로젝트 파일을 수정하지 않는다.
- 중요한 변경 전에는 구현 방향, 변경할 파일, 영향 범위와 핵심 코드 형태를 먼저 설명하고 사용자 확인을 받는다.
- API, 데이터 구조 또는 DB 스키마 변경 여부를 수정 전에 명확히 알린다.
- DB 스키마 변경, 데이터 이관, 데이터 수정·삭제 SQL은 대상 DB와 영향 범위를 설명하고 명시적 승인을 받은 뒤에만 실행한다.

## 최소 구현 원칙

코드를 작성하기 전에 다음 순서로 검토한다.

1. 현재 요구사항에 실제로 필요한가?
2. 프로젝트에 같은 역할의 코드, 패턴 또는 도메인 규칙이 이미 있는가?
3. Java 표준 라이브러리나 Spring의 기본 기능으로 해결할 수 있는가?
4. 이미 설치된 의존성으로 해결할 수 있는가?
5. 그래도 필요할 때만 가장 작고 명확한 구현을 추가한다.

- 요청하지 않은 확장 지점, 인터페이스, 팩토리, 래퍼 또는 설정 계층을 만들지 않는다.
- 현재 요구사항을 위한 구현체나 호출자가 하나뿐이라면 추상화를 추가하지 않는다. 명확한 두 번째 사용 사례가 생길 때 도입한다.
- 작업 범위 밖의 리팩터링, 선제적 일반화 및 새 프로덕션 의존성 추가를 피한다.
- 기존 코드와 프로젝트 패턴을 먼저 재사용하고, 가장 작은 검토 가능한 diff를 선호한다.
- 코드 줄 수를 줄이기 위해 가독성, 도메인 의미 또는 기존 모듈 경계를 희생하지 않는다.
- 새 프로덕션 의존성이 필요하면 추가 전에 필요성, 기존 대안과 영향 범위를 설명하고 승인을 받는다.

## 백엔드 안전 기준

- 최소 구현을 이유로 입력 검증, 인증·인가, 트랜잭션, 데이터 무결성, 오류 처리, 캐시 일관성, 동시성 또는 관측 가능성을 생략하지 않는다.
- 버그 수정 전 관련 호출 경로와 공통 원인을 확인한다. 증상별 중복 패치보다 모든 호출자가 통과하는 원인을 한 번 수정한다.
- `hanyang-user-api`와 `hanyang-admin-api`가 `hanyang-core`와 PostgreSQL·Redis 상태를 공유한다는 점을 고려한다.
- 외부 API 수집 실패나 빈 응답이 기존 정상 데이터 또는 캐시를 덮어쓰지 않도록 한다.
- 의도적으로 단순한 구현을 선택했지만 알려진 한계가 있다면, 한계와 교체 조건을 코드 또는 작업 결과에 명시한다.

## 검증

- 변경한 동작에 대응하는 기존 테스트를 수정하거나 필요한 테스트를 추가한다.
- 관련 모듈 테스트를 먼저 실행하고, 공통 모듈이나 모듈 간 동작을 변경했다면 전체 테스트를 실행한다.
- 기본 전체 검증 명령은 `./gradlew test`이다.
- 패키징 또는 실행 구성에 영향을 주는 변경은 `./gradlew bootJar`도 실행한다.
- 실행한 검증, 실패한 검증과 남은 위험을 최종 응답에 명확히 보고한다.

## Git 및 보안

- 사용자의 명시적 요청 없이 커밋, 원격 push, PR 생성·병합·닫기 또는 force push를 하지 않는다.
- 기존 사용자 변경을 덮어쓰거나 되돌리지 않는다.
- `.env`, 자격 증명, 토큰, 운영 인증서 및 비밀 값의 내용을 출력하거나 커밋하지 않는다.
